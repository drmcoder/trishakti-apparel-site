# Drop real assets here

Replace the on-site placeholders by adding real files to `public/` and wiring them in.

## Needed (see docs/site-data.md)
- [ ] `logo.svg` — transparent logo (then update `components/Logo.js` to use it)
- [ ] Factory photos: sewing line, cutting room, QC, packing, building exterior
- [ ] Product photos: t-shirts, polos, stitch close-ups
- [ ] Team / leadership photo(s)
- [ ] `og-image.jpg` (1200×630) for social sharing

Until real photos are provided, the site shows tasteful placeholders — **no stock
photos are passed off as the real factory** (per the project's hard rules).

Reference images in code as `/assets/<file>` (files in `public/` are served from the root).
